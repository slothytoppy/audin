package audio_lib

import "core:container/queue"
import "core:fmt"

/*
@(required_results)
append_song :: proc(queue: ^queue.Queue, song_name: string) ->queue{
	queue.append(queue, song_name)
  return queue
}
*/
